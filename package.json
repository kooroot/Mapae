{
  "name": "mapae",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "typecheck": "tsc -p packages/shared/tsconfig.json && tsc -p packages/delegation/tsconfig.json && tsc -p apps/agent/tsconfig.json && tsc -p apps/seller/tsconfig.json && tsc -p apps/delegation-lab/tsconfig.json && tsc -p apps/facilitator-erc7710/tsconfig.json && tsc -p apps/delegated-agent/tsconfig.json && tsc -p apps/delegated-seller/tsconfig.json",
    "test:ts": "bun test packages/shared/src packages/delegation/src",
    "test:contracts": "cd contracts && forge test -vvv",
    "check": "bun run typecheck && bun run test:ts && bun run test:contracts"
  },
  "dependencies": {
    "viem": "^2.55.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
